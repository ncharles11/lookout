# Lookout backend configuration
# Copy to .env and adjust as needed. NEVER commit a real .env.

# PostgreSQL / TimescaleDB connection string
DATABASE_URL=postgresql://lookout:lookout@localhost:5432/lookout

# Maximum number of probes running concurrently
PROBE_CONCURRENCY=10
